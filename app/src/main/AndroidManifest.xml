<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health_services_locating">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Health_services_locating">
        <activity android:name=".splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".health_centres_in_rubaga" />
        <activity android:name=".health_centres_in_nakawa" />
        <activity android:name=".ratingbar" />
        <activity android:name=".logout" />
        <activity android:name=".types_of_services" />
        <activity android:name=".health_centres_in_kawempe" />
        <activity android:name=".health_services" />
        <activity android:name=".signup" />
        <activity android:name=".signin" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>